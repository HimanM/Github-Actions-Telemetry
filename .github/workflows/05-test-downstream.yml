name: 05 - Test Downstream Dependent
on:
  workflow_run:
    workflows: ["04 - Test Upstream Dependent"]
    types: [completed]
jobs:
  run-downstream:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: echo "I run after 04 finishes!"
